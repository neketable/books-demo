<div class="container h-100">
	<div class="htop">
		<h2 class="text-center">Редактирование спикера: {{model.lastName}} {{model.firstName}}</h2>
		{{speaker-form speaker=model onsubmit=(action "saveSpeaker")}}
	</div>
</div>